# -https-saucedemo.com-automation-testing-playright
🧪 Saucedemo Automation Tests (Playwright + Java)Automatyczne testy E2E dla strony Saucedemo przy użyciu Playwright i JUnit 5. Raporty generuje Allure Report.🚀 Uruchomienie:mvn clean test — wykonuje testy.mvn allure:serve — otwiera raport.Wymaga Java 24+ oraz Maven.
